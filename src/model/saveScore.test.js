import { saveScore } from './saveScore';

describe('saveScore', () => {
  it('saves score into local storage', () => {
        afterEach(() => { 
            localStorage.clear();
        })
    });
});