const startGameButton = document.querySelector('newGameButton');
console.log(startGameButton);

startGameButton.addEventListener('click', (e) => {
  console.log(e);
});

export default startGameButton;
